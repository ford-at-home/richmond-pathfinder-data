# Public environment variables

This site is static. It does not call a backend, and it does not read secrets
in client code.

No environment variables are required to build or run the migrated reports.
